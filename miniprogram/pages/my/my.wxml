

<user_home wx:if="{{id}}" userInfo="{{userInfo}}" 
  scroll="{{scroll}}" nav="{{nav}}" />


<button wx:else open-type="getUserInfo" class="auth_button"
bindgetuserinfo="onGetUserInfo" plain hover-class="hover-class"
style="color:white;border:3rpx solid white;">请先授权登录</button>

